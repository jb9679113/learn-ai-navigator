# Vercel Postgres 连接字符串（部署到 Vercel 后自动注入）
# 本地开发时需在 Vercel 项目面板的 Storage 中获取连接字符串
POSTGRES_PRISMA_URL="your-postgres-connection-string"

# 可选：兼容 Prisma 默认行为
DATABASE_URL="your-postgres-connection-string"

# 后台管理密码（默认 admin123）
ADMIN_SECRET="admin123"

# NextAuth 密钥（可选，目前不需要）
NEXTAUTH_SECRET=""
